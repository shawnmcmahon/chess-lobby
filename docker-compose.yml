services:
  chess-engine:
    build:
      context: .
      dockerfile: apps/chess-engine/Dockerfile
    ports:
      - "8080:8080"
    environment:
      ENGINE_API_KEY: dev-secret
      STOCKFISH_PATH: /usr/games/stockfish
